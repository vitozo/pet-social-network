let initialState = {
    friends: [
        {id: 1, name: 'Andrew'},
        {id: 2, name: 'Tina'},
        {id: 3, name: 'Valera'}
    ]
};

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;
